<i class="btn-open icon-expand-alt"/>
<div class="content">

	<i class="btn-close icon-remove"/>
	
	<div class="separator"/>

	<div class="inputs">
		{{# each inputs }}
	
			{{# if is_button }}
				<button
					name="{{ name }}"
				>
					{{ name }}
				</button>
			{{/ if }}

			{{# if is_checkbox }}
				<input
					type="checkbox"
					name="{{ name }}"
					{{# if checked }}checked="checked"{{/ if }}
				>{{ name }}
			{{/ if }}

		{{/ each }}
	</div>

</div>
